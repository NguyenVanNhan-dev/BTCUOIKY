<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Màu nền cho Button -->
    <corners android:radius="0dp" />    <!-- Góc vuông -->
    <stroke android:width="2dp" android:color="#000000" /> <!-- Viền đen, tùy chọn -->
</shape>
